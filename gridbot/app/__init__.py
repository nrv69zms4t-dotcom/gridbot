"""GridBot desktop app — pywebview GUI layer over the gridbot engine.

Modules:
    paths  — data/web directory resolution (dev vs PyInstaller-frozen)
    api    — backend bridge exposed to JS (pywebview js_api)
    main   — entry point: window, --selftest, --smoke, --debug
"""

__version__ = "1.0.0"
